# 滑卡与来源多样性优化

## 交互

顶部集中发现、收藏、设置；姓氏入口常驻卡片上方。主界面固定在可见视口内，安全区留白，收藏和设置独立滚动。卡片只显示名字、简短释义和出处摘要，完整原文、人工核验状态、历史热门提醒与五行参考放到详情面板。

卡片手势使用指针捕获及逐帧位移，距离或速度达标后完成选择；轻微斜滑不取消，纵向拖动不误收藏，多指及系统取消会回弹。离场卡单独保留，下一张同步呈现，不用动画锁拦截快速操作。卡片区域使用禁用原生触摸动作的样式，输入框至少16像素；没有全局禁止辅助缩放。

前端只消费库存。低于16张时每次获取至多8张，网络请求12秒超时并指数退避；切换条件立即取消旧请求并使用版本号隔离晚到响应。发现页不可见时停止补给心跳。收藏本机持久化后异步写入，每张卡保留自己的任务编号，失败可重试。

## 生产与分配

每个生产任务只处理一本古籍的25个连续取字候选，最多8路并发；九部古籍按游标轮换，跨活跃条件逐轮分配任务。合格结果每批独立入库，不等待其他请求。失败按来源退避，已成功批次不丢弃；已审原文候选持久化排除，不重复付费审查。

模型判断是否适合作现代姓名，结合姓氏、原文审稿，并给出释义和自由文化标签。必须是召回池中的编号且评分不低于75。模型失败不能退回原文碎片。已核验拼音、字属性和历史热度在本地附加；模型不判断宝宝五行缺失。

发卡优先选择最近36次曝光及当前预留中较少的来源，然后减少相邻同书、最近重字，再参考质量分和随机同分排序。每次同书最多2张，只有一部书先完成时先给少量卡片，给其他来源留位置。来源保证针对有合格库存的书籍；不为凑齐九部书而降低适名质量，也不承诺有限语料永不耗尽。

## 队列与预算

普通条件按姓氏、字数、规范化避用字共享池，不同姓氏不共享未经姓氏审读的结果。固定字或关键词的池额外包含用户身份，互不混用。物料不因某位用户看到而全局删除，其他用户仍能复用。

目标库存为每位活跃用户72个未看名字，含已预留卡片；没有活跃用户或库存足够就不派新任务。最多32个近期活跃条件，8个在途生产任务。共享池单池1200条软上限，临界时仅回收所有活跃消费者均已看、且无人预留的物料；最多存在一轮在途超调。用户离开后90秒停止新增任务，已派发的请求可完成并留存。

固定字池5分钟没有访问回收，共享池7天没有访问回收；曝光独立保存。临时池回收后晚到结果不会重新创建队列。每天最多派发480批，预算存SQLite，重启不会归零，UTC零点切换统计日。上限包含失败与空召回调度，宁可保守限额，不无限烧额度。生产错误和额度状态通过提示与管理员来源统计呈现。

## 身份、曝光与可靠性

设备ID是带随机安装盐的粗粒度浏览器指纹摘要，固定保存在本机，不采集画布或字体。服务端身份同时绑定随机会话密钥，指纹不是认证。清理存储、换浏览器或设备会产生新身份，不宣称可靠识别同一个自然人。

SQLite事务原子创建投递租约，同一身份同名只能有一个有效预留，防止并发拉卡重复；各标签页独立缓存，刷新保留当前卡。请求编号提供短期幂等，网络重试不重复分配。客户端真正呈现卡片后批量确认曝光，预加载不计已看。未看租约5分钟到期可恢复，活跃页面20秒续租一次。

每个用户一个16千字节、6哈希布隆过滤器；阴性直接放行，阳性必须查询精确曝光表。布隆误判只增加数据库查询，不会浪费名字。位图与精确曝光在同一事务更新，重启仍保留。投递证明保留30天以支持租约过期后的离线确认；超过此时间才回传的离线曝光不保证追认。

任务和收藏仍通过原有会话所有权检查。所有外部模型请求仅从后端发送，管理查询不回显接口密钥，数据库运行数据和真实配置不得提交到Git。新增表自动创建，不重建原数据。

## 验收及限制

使用临时数据库测试来源轮换、多用户复用、并发唯一、幂等、布隆误判、持久化曝光、晚到确认、临时池过期、失败退避、库存及预算控制；使用模拟接口的真实Chrome测试同帧连滑、真实触摸斜滑、双指、取消、手机横屏、小屏、条件立即切换、收藏归属和刷新。

默认单工作进程。扩为多实例前需增加跨进程生产租约或独立调度服务，不能直接增加Uvicorn工作进程。冷启动与库存耗尽仍受模型耗时限制，不能承诺任何情况下零等待。生产吞吐要根据真实合格率、用户滑动速度和接口额度调整。真实iOS设备的手感、字体和浏览器边缘返回手势仍需人工验收。
