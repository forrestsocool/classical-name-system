# 实现审查与优化记录

审查日期：2026-09-10。

| 问题 | 修复与验证 |
| --- | --- |
| 公共历史列出所有任务，取得编号即可删除或调用模型 | 增加会话归属校验，过滤历史，保护任务读写、收藏和反馈；增加两个会话之间的真实接口回归测试 |
| 测试修改项目数据库、复核状态导致重复测试失败 | 每个用例使用独立临时库；连接退出时关闭；不再用重建数据清除测试痕迹 |
| 建库清空业务数据 | 检测任务、收藏、反馈及模型调用记录，有记录时拒绝重建 |
| 模型绕过固定字、避用字与排除名单 | 与本地候选共用约束，按字数统计检查重复固定字 |
| 同句、跨句取字误标，重复字可重复使用同一位置 | 校验句界和不同字符位置；生成逐字位置列表 |
| 单字名永远没有候选 | 从已审核连续词组提取单字，保留原文偏移并再次校验 |
| 诗经篇名仅存在于报告 | 导入器直接关联正式篇名；保留原文重复标题风险；校验每个分区的配置数量 |
| 周易字形规则误判 | 匹配必须覆盖当前目标字，修复“终日乾乾”和“事之干”；66处建议乾、13处保留干 |
| 出生时间被浏览器时区转换 | 提交所选地区的原始本地时间；拒绝夏令时不存在或重复的模糊时间；日界版本与参数一致 |
| 脱敏后仍保留农历年月日 | 持久化摘要移除农历日期，文档说明四柱的剩余信息风险 |
| 动态未知路径和来源地址造成内存持续增长 | 未知监控路径归一化，限制限流来源数量并清理过期记录 |
| 每次就绪探测扫描完整数据库 | 就绪检查采用轻量数据库查询，完整性检查保留于部署检查 |
| 低流量时告警分母截断到每秒1次 | 改用微小非零分母，避免压低错误率和平均耗时 |
| 收藏比较、模型结果缺少网页入口 | 补充展示及操作入口，起名期间禁用重复提交，修复移动端管理按钮换行 |
| 文档把本机短时压测当作生产验收 | 恢复真实生产验收未完成状态，说明网络、HTTPS和告警投递条件 |

字形核对来源：[中国哲学书电子化计划·乾卦文言](https://ctext.org/book-of-changes/gan1/ens)、[乾卦象传](https://ctext.org/book-of-changes/gan/ens)。

## 仍需外部验收的事项

真实模型服务需要有效密钥；目前验证使用本地模拟服务。生产环境尚未提供主机、正式域名和告警接收渠道，不能声称已完成上线验收。古籍正文、典故语义及汉字五行资料仍需扩大人工校对范围；当前资料池规模有限，排序分数不代表姓名质量的客观结论。

会话密钥属于持有者凭据，没有账户找回能力；部署时使用 HTTPS。代码中的会话归属表仅存密钥哈希。当前内存限流和模型并发控制适用于单进程。

收藏夹编号使用会话密钥的 SHA-256 摘要，避免把原始凭据放入查询参数和访问日志。

跨时区八字的年柱和月柱按统一的东八区节气时刻判断，日柱和时柱按用户选择的当地时间判断；增加同一时刻跨地区的年月柱一致性回归用例。

## 本地验证记录

27项单元及接口回归测试连续两轮通过，测试前后项目数据库哈希一致。真实 Edge 浏览器验证双字名、单字名、收藏、比较、历史和390像素窄屏布局，未出现网页异常。独立运行库上四类场景各10次请求全部成功；3并发下检索 P95 约40毫秒，起名 P95 约231毫秒，10个压测任务全部清理。该结果仅代表本机短时测试。

补充跨时区用例后，最终28项测试通过。网页验证脚本为 `脚本/验证网页.py`，需要额外安装 `playwright` 并使用已安装的 Edge；仅对独立测试运行库执行。
