# 微信小程序与 PostgreSQL 改造方案

状态：本分支实施方案；不执行线上迁移或云端发布。

## 部署边界

```text
微信小程序 → wx.cloud.callFunction → CloudBase nameGateway
                                        │ 可信微信身份 + HMAC 签名 / HTTPS
                                        ▼
                            自有服务器 FastAPI /internal/v1/dispatch
                                        │
管理浏览器 → /admin → /api/admin/* ────── PostgreSQL
                                        ▲
                           独立常驻生产进程 → 模型服务
```

选择云函数作为第一版入口，无需同时维护云托管。微信侧只有展示、输入和云调用；云函数仅负责平台身份、动作白名单、请求大小限制、超时及签名转发，不持有模型密钥、数据库或管理权限。业务服务器与生产进程共享 PostgreSQL 和模型配置文件。API 进程不启动生产线程。

小程序接入不直接使用自有服务器域名；云函数出口仍需能访问服务器 HTTPS。云开发接入不等于免除小程序自身的备案、发布审核，也不会改变自有服务器所在地区的域名要求。上线前按实际部署地区与微信后台要求办理。技术依据：[CloudBase 小程序云函数](https://docs.cloudbase.net/recipes/add-cloud-function-wechat-miniprogram)；备案范围依据：[工信部移动互联网应用程序备案通知](https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm)。

## 身份与数据隔离

- 云函数只从 `cloud.getWXContext()` 取得 APPID/OPENID，拒绝缺失身份及非指定 APPID；不采信 event 中的身份、地址、路径、请求头。
- 服务器验证原始请求体 HMAC-SHA256，绑定版本、时间戳、随机 nonce 和 body hash；五分钟时效，数据库唯一 nonce 防止跨进程重放。
- 用户主键由 APPID + OPENID 的 SHA256 派生，收藏、投递历史和反馈均以服务器取得的用户为边界。浏览器指纹和自选会话不再作为身份。
- 网关只允许拉卡、收藏列表、收藏、取消收藏、比较和反馈。管理 API 使用独立管理密钥，管理页不打包到小程序。
- 网关动作经过严格参数验证；限流按可信用户记录在 PostgreSQL，避免所有云函数请求共享同一个出口 IP 配额。

## 简化库存与生产

- 保留按姓氏和名字长度的生产档案，以继续让模型检查完整姓名的音义。初始档案来自配置；新用户条件登记为持久档案，独立生产进程轮换处理，无需用户持续在线。
- 固定字、避用字只过滤已入库名字，不再创建临时私有池；不提供自由关键词即时生成。稀有条件没有库存时明确提示等待或放宽条件。
- 单常驻进程顺序轮换档案和来源，一批最多 25 个候选，已核验出处与原有模型质量规则复用。合格候选立即入库，失败退避，重启后继续。
- PostgreSQL advisory lock 防止重复生产进程；持久化每日派发额度（含失败批次）、来源进度及已审名字；不需要 Redis、消息队列、活跃心跳、布隆过滤器或租约。
- 库存持久积累，不随用户消费删除。同一用户按完整姓名精确去重。发卡即记入投递历史，换取简洁可靠的并发行为；同一个请求编号重试可取回原批次。未看的预取卡也计已投递，这是相对旧版本的明确取舍。
- 小程序一次取 8 张，保留当前缓存，按钮/滑动跳过或收藏；收藏失败保留当前卡供重试。请求失败沿用请求编号，防止网络重试跳过整批。
- 管理员能增加/暂停生产档案、查看来源进度、失败、库存与每日额度；全天运行仍受每日模型预算约束。

## PostgreSQL 与迁移

- 新运行库全部使用 PostgreSQL。SQLite 仅作为一次性迁移输入、旧版离线工具和回归测试的数据源。
- 迁移命令先做只读 SQLite 在线快照，按明确表顺序导入资料和历史业务，保留主键与复核结论，重置 identity sequence，验证表行数，在一个事务中提交。目标库有旧表时拒绝覆盖，重复迁移不写入。
- 旧浏览器收藏与任务以历史表保留供管理员统计；不猜测旧匿名身份与微信 OPENID 的对应关系。旧临时租约、指纹与布隆表不导入新业务。
- 新业务使用独立 `app_*` 表，具备外键、唯一约束、原子发卡与幂等回执。现有已审物料保留在历史表，新的公开库存经独立生产流程建立。
- 生产迁移前备份 SQLite；切换后用 `pg_dump` 备份 PostgreSQL。回退需保留 PostgreSQL 新用户数据，不能直接覆盖回旧 SQLite。

## 交付与验收

交付原生小程序、云函数、PostgreSQL 服务与生产进程、独立后台、迁移/备份工具、Compose 和配置模板、部署步骤。旧网页与旧服务源码保留供历史回归，新启动脚本、Docker 镜像及生产部署入口均指向新服务，不挂载旧选名页面或开放旧业务路由。

验收覆盖签名篡改/重放/过期、跨用户隔离、并发拉取与幂等、库存条件、预算与生产重启、数据库迁移及事务回滚；云函数与小程序脚本通过 Node 测试。实际微信开发者工具预览、真机云函数链路、模型付费调用和线上迁移需要真实部署配置，不能以模拟测试宣称完成。
