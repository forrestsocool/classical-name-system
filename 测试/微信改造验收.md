# 微信小程序改造验收记录

日期：2026-09-17。分支：codex/wechat-cloudbase-postgresql。

## 已执行

- 原有 Python 回归测试 54 项通过，覆盖古籍资料、候选筛选、模型输出校验、旧版会话与旧队列。共用召回逻辑改动保持通过。
- PostgreSQL 集成测试 13 项通过，使用本机隔离的 PostgreSQL 17.6，测试创建并销毁独立数据库。覆盖迁移行数/序列/拒绝覆盖/失败回滚、网关篡改/过期/重放、Node 与 Python 的中文载荷签名互通、并发发卡幂等、过滤字次数、用户隔离、管理边界、停用出处、预算、失败重试与生产进程锁。
- Node 测试 8 项通过，覆盖可信身份与固定转发目标、动作限制、请求大小、异常脱敏、客户端失败重试、收藏连点、身份缓存隔离、条件切换。
- Python 编译、JavaScript 语法、项目 JSON 与部署 YAML 检查通过；原版资料库部署检查通过。
- Chrome 无头浏览器在 1440px 桌面及 390px 手机视口检查管理后台：登录、生产档案添加、资料复核列表、退出通过，无页面脚本异常或横向溢出。
- 迁移试运行导入 10,987 个古籍/辅助片段、2,099 条年号、5,907 个汉字；源 SQLite 由只读快照读取，未修改仓库数据库。
- PostgreSQL 备份脚本实际生成 pg_dump 自定义格式文件，并用 pg_restore 还原到另一隔离数据库，核对恢复后片段数为 10,987。

## 未在本地宣称完成

- 本机没有 Docker，Compose 完成配置解析与继承检查，尚未执行容器镜像构建和容器部署。
- 未配置真实微信 AppID、云环境和外网 HTTPS 接入，尚未进行微信开发者工具编译、真机云调用和发布审核。
- 模型调用均模拟，未进行付费模型验收；未迁移线上数据库、上传云函数或发布小程序。

运行命令与目标环境验收步骤见 [生产部署说明](../部署/生产部署说明.md)。
