"""旧业务接口测试的生成桩；智能筛选有独立的模型边界测试。"""
from 后端.候选生成 import 生成基础候选


def 生成桩(连接, 请求):
    return 生成基础候选(连接, 请求["姓氏"], 请求["名字长度"], [],
        请求["必须包含"], 请求["避用字"], 请求["关键词"],
        请求["随机种子"], [], 排除名字=请求["排除名字"])
